# Extra pip constraints applied automatically by install.sh when the target
# system has glibc < 2.28 (e.g. CentOS 7 / RHEL 7 / Amazon Linux 2).
#
# Modern releases of these packages only ship manylinux_2_28 wheels, which
# cannot be installed there; pip would fall back to building from source and
# fail without a full C/C++ toolchain. Each pin is the newest release that
# still ships glibc-2.17-compatible binary wheels.
numpy==2.2.6          # 2.3+ dropped manylinux_2_17 wheels
pyarrow==20.0.0       # 21+ dropped manylinux_2_17 wheels (via lancedb)
onnxruntime==1.16.3   # 1.17+ dropped manylinux_2_17 wheels (via chromadb)
contourpy==1.3.2      # 1.3.3+ dropped manylinux_2_17 wheels (via matplotlib)
pillow<12.3           # 12.3+ dropped manylinux_2_17 wheels
pyzmq==26.4.0         # 27+ dropped manylinux_2_17 wheels
greenlet==3.2.5       # 3.3+ dropped manylinux_2_17 wheels
scipy==1.16.3         # 1.17+ dropped manylinux_2_17 wheels
pandas==2.3.2         # 3.x dropped manylinux_2_17 wheels (optional extra)
tiktoken==0.11.0      # 0.12+ dropped manylinux_2_17 wheels (via litellm)
google-re2==1.1       # newer builds ship manylinux_2_28-only wheels
# litellm 1.92+ ships only a Rust-backed manylinux_2_28 wheel and would need
# a full Rust toolchain to build from source; 1.91.5 is pure Python.
litellm==1.91.5
