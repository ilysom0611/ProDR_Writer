# Single source of truth for runtime dependencies.
# Consumed by pyproject.toml's dynamic `dependencies` (setuptools reads this
# file). On glibc < 2.28 systems, install.sh additionally applies
# requirements-oldglibc.txt as pip constraints.
crewai>=1.12.0,<2
litellm>=1.65.0,<2
python-docx>=1.2.0,<2
pydantic>=2.0
typer>=0.9.0
rich>=10.11.0
matplotlib>=3.5.0,<4
PyYAML>=6.0
fastapi>=0.110,<1
uvicorn>=0.29,<1
